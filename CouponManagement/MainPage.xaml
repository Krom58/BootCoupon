<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CouponManagement.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CouponManagement"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
  <Page.Resources>
    <Style x:Key="MenuButtonStyle" TargetType="Button">
      <Setter Property="Padding" Value="8"/>
      <Setter Property="Background" Value="{ThemeResource SystemControlBackgroundAccentBrush}"/>
      <!-- other setters -->
    </Style>
  </Page.Resources>
    <Grid>
        <StackPanel Orientation="Vertical" Spacing="10" Margin="20">
            <Button x:Name="CouponDefinitionButton" 
                    Content="จัดการคำนิยามคูปอง" 
                    Click="CouponDefinitionButton_Click"
                    Style="{StaticResource MenuButtonStyle}"/>
            <Button x:Name="GeneratedCouponsButton"
                    Content="ดูคูปองที่สร้างแล้ว"
                    Click="GeneratedCouponsButton_Click"
                    Style="{StaticResource MenuButtonStyle}"/>
        </StackPanel>
    </Grid>
</Page>
